# driftbird-night

This directory exists so game pages can be generated automatically from `src/games/*`.

Runtime game logic is currently shared via `driftbird` implementation.
Assets/theme is defined by local `assets.dsl` and metadata in `game_spec.json`.
